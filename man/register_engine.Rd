% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extend-causaldisco-algs.R
\name{register_engine}
\alias{register_engine}
\title{Register a New Engine}
\usage{
register_engine(name, make_runner_fn, pkgs = character(0))
}
\arguments{
\item{name}{Engine name (string). Cannot be one of the built-in engine
names ("bnlearn", "causalDisco", "pcalg", "tetrad").}

\item{make_runner_fn}{A function implementing the engine. See Details.}

\item{pkgs}{Character vector of package names required by the engine.
Checked, with an informative error if missing, before \code{make_runner_fn} is
called.}
}
\description{
The built-in engines ("bnlearn", "causalDisco", "pcalg", "tetrad") are
backed by R6 search classes (\link{BnlearnSearch}, \link{CausalDiscoSearch},
\link{PcalgSearch}, \link{TetradSearch}) that \code{\link[=make_runner]{make_runner()}} knows how to build and
configure. \code{register_engine()} lets you plug in a backend for a package
that isn't one of the built-ins, so that it can be selected by name from
\code{\link[=make_runner]{make_runner()}} the same way as a built-in engine.
}
\details{
\code{make_runner_fn} must accept \code{alg} and \code{...}, and return a runner: a list
with two elements,
\itemize{
\item \code{set_knowledge}, a function that takes a single \code{Knowledge}
object and configures background knowledge on the underlying search, and
\item \code{run}, a function that takes a single data frame, runs the search,
and returns the algorithm's result using \code{\link[=as_disco]{as_disco()}}.
}
\code{\link[=make_runner]{make_runner()}} calls \code{make_runner_fn} with \code{alg}, \code{test}, \code{alpha},
\code{score}, and any additional arguments it was itself called with. Pick up
whichever of these your engine needs by naming them explicitly, and let
\code{...} absorb the rest.
}
\seealso{
Other Extending causalDisco:
\code{\link[=as_disco]{as_disco()}},
\code{\link[=distribute_engine_args]{distribute_engine_args()}},
\code{\link[=list_registered_engines]{list_registered_engines()}},
\code{\link[=list_registered_tetrad_algorithms]{list_registered_tetrad_algorithms()}},
\code{\link[=make_method]{make_method()}},
\code{\link[=make_runner]{make_runner()}},
\code{\link[=new_disco_method]{new_disco_method()}},
\code{\link[=register_tetrad_algorithm]{register_tetrad_algorithm()}},
\code{\link[=reset_engine_registry]{reset_engine_registry()}},
\code{\link[=reset_tetrad_alg_registry]{reset_tetrad_alg_registry()}}
}
\concept{Extending causalDisco}
\concept{extending_causalDisco}
